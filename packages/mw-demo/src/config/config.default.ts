
import {
  initialConfig,
  initialMiddlewareConfig,
} from '../lib/config'


export const demoConfig = {
  ...initialConfig,
}

export const demoMiddlewareConfig = {
  ...initialMiddlewareConfig,
}

